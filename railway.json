{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "echo \"Starting on port $PORT\" && python manage.py migrate && gunicorn config.wsgi --bind 0.0.0.0:$PORT --log-file - --error-logfile - --log-level debug",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}